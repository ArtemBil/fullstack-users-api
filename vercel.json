{
  "version": 2,
  "builds": [
    { "src": "server/index.ts", "use": "@now/node-server" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "server/index.ts" },
    { "src": "/(.*)", "dest": "client/build/index.html" }
  ]
}